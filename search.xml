<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>原生 DOM 和 虚拟 DOM 详解</title>
      <link href="/2021/07/08/p007/"/>
      <url>/2021/07/08/p007/</url>
      
        <content type="html"><![CDATA[<h2 id="DOM-是什么？"><a href="#DOM-是什么？" class="headerlink" title="DOM 是什么？"></a>DOM 是什么？</h2><p>DOM 全称 Document Object Model，文档对象模型。<br>DOM 是浏览器解析 HTML&#x2F;XML 文档的结构化对象模型，它将网页内容表示为有层次的节点树，每个节点对应文档中的一个元素（如标签、属性、文本等）。<br>DOM 不是单一对象，它是由许多节点对象组成的树结构。<br>比如如下网页代码：</p><pre><code class="hljs html"><span class="hljs-meta">&lt;!Doctype <span class="hljs-keyword">html</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">html</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">head</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">title</span>&gt;</span>My Title<span class="hljs-tag">&lt;/<span class="hljs-name">title</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">head</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">body</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">a</span> <span class="hljs-attr">href</span>=<span class="hljs-string">&quot;https://www.xxx.com&quot;</span>&gt;</span>My Link<span class="hljs-tag">&lt;/<span class="hljs-name">a</span>&gt;</span>        <span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>My Header<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span>    <span class="hljs-tag">&lt;/<span class="hljs-name">body</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">html</span>&gt;</span></code></pre><p>转换为 DOM 树为：</p><img src="/2021/07/08/p007/image-1.png" class="" title="alt"><p>这是简化的 DOM 树，实际的 DOM 树非常复杂庞大，因为它继承自一系列的原型链。</p><p>HTML 元素类的总体继承关系如下：</p><img src="/2021/07/08/p007/image-2.png" class="" title="alt"><p>DOM 的特点：</p><ul><li>存在于浏览器内存中</li><li>采用父子节点关系的层次结构</li><li>包含多种节点类型</li><li>提供操作节点的方法和属性，js 可以修改 DOM 并实时反映在页面上</li></ul><h2 id="DOM-和-js-的关系"><a href="#DOM-和-js-的关系" class="headerlink" title="DOM 和 js 的关系"></a>DOM 和 js 的关系</h2><p>DOM 是由浏览器实现的，它不是 js 的一部分，DOM 提供了一种编程接口，允许 js 动态访问、修改、添加、删除网页内容、结构、样式。</p><h2 id="DOM-解析"><a href="#DOM-解析" class="headerlink" title="DOM 解析"></a>DOM 解析</h2><p>DOM 解析是指浏览器将 HTML&#x2F;XML 文档转换为 DOM 树的过程。</p><p>具体过程为：浏览器接受 HTML 文档 -&gt; HTML 解析器将 HTML 转换为 tokens -&gt; tokens 转换为 nodes -&gt; nodes 被组织成 DOM 树。</p><h3 id="HTML-解析器是如何工作的？"><a href="#HTML-解析器是如何工作的？" class="headerlink" title="HTML 解析器是如何工作的？"></a>HTML 解析器是如何工作的？</h3><p>HTML 解析器是浏览器的一部分，它负责将 HTML 字节流转换为 DOM 树。</p><img src="/2021/07/08/p007/image-4.png" class="" title="alt"><p>网络进程接收到响应头后根据 <code>content-type: text-html</code> 判断是 HTML 文件，浏览器为该请求创建一个渲染进程，渲染进程和网络进程间会建立一个共享数据的通道，网络进程往通道里放数据，渲染进程不断读取数据，并同，时将数据给 HTML 解析器，解析器动态解析数据，接收多少解析多少，而不是等整个文档加载完成后再解析的。</p><p>字节流转换为 DOM 需要三个阶段：</p><ul><li>第一个阶段通过分词器将字节流转换为 Token</li><li>第二个和第三个阶段是同步进行的，需要将 Token 解析为 DOM 节点，并将 DOM 节点添加到 DOM 树中</li></ul><img src="/2021/07/08/p007/image-5.png" class="" title="alt"><p>HTML 解析器维护了一个 Token 栈结构，分词器将字节流转换为一个个 Token，Token 分为 TagToken 和 文本 Token，TagToken 又分为 StartTag 和 EndTag，具体规则如下：</p><ul><li>如果压入到栈中的是StartTag Token，HTML 解析器会为该 Token 创建一个 DOM 节点，然后将该节点加入到 DOM 树中，它的父节点就是栈中相邻的那个元素生成的节点。</li><li>如果分词器解析出来是文本 Token，那么会生成一个文本节点，然后将该节点加入到 DOM 树中，文本 Token 是不需要压入到栈中，它的父节点就是当前栈顶 Token 所对应的 DOM 节点。</li><li>如果分词器解析出来的是EndTag 标签，比如是 EndTag div，HTML 解析器会查看 Token 栈顶的元素是否是 StarTag div，如果是，就将 StartTag div 从栈中弹出，表示该 div 元素解析完成。</li></ul><p>通过分词器产生的新 Token 就这样不停地压栈和出栈，整个解析过程就这样一直持续下去，直到分词器将所有字节流分词完成。</p><h2 id="虚拟-DOM"><a href="#虚拟-DOM" class="headerlink" title="虚拟 DOM"></a>虚拟 DOM</h2><p>虚拟 DOM 是一个轻量级的 js 对象，是真实 DOM 的抽象表示。</p><p>特点：</p><ul><li>不是真实的浏览器 DOM 元素</li><li>比真实 DOM 更轻量，操作更快</li><li>使用 js 对象树来描述 DOM 结构</li><li>减少直接操作真实 DOM 的次数</li></ul><h3 id="虚拟-DOM-工作原理"><a href="#虚拟-DOM-工作原理" class="headerlink" title="虚拟 DOM 工作原理"></a>虚拟 DOM 工作原理</h3><p>初始渲染：</p><img src="/2021/07/08/p007/image-6.png" class="" title="alt"><p>状态更新：</p><img src="/2021/07/08/p007/image-7.png" class="" title="alt"><h3 id="为什么需要虚拟-DOM？"><a href="#为什么需要虚拟-DOM？" class="headerlink" title="为什么需要虚拟 DOM？"></a>为什么需要虚拟 DOM？</h3><p>直接操作 DOM，每次操作都会触发重排&#x2F;重绘，使用虚拟 DOM，一次性更新 DOM，只触发一次重排</p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>Vue2 源码分析</title>
      <link href="/2021/07/01/p006/"/>
      <url>/2021/07/01/p006/</url>
      
        <content type="html"><![CDATA[<h2 id="源码目录结构"><a href="#源码目录结构" class="headerlink" title="源码目录结构"></a>源码目录结构</h2><img src="/2021/07/01/p006/image-1.png" class="" title="alt"><ul><li>src<ul><li>compiler  编译相关</li><li>core  核心代码</li><li>platforms  不同平台的支持</li><li>server  服务器渲染</li><li>sfc  解析.vue文件</li><li>shared  共享代码</li></ul></li></ul><h2 id="runtime-complier-和-runtime-only-的区别"><a href="#runtime-complier-和-runtime-only-的区别" class="headerlink" title="runtime+complier 和 runtime only 的区别"></a>runtime+complier 和 runtime only 的区别</h2><pre><code class="hljs js"><span class="hljs-comment">// runtime+compiler的 main.js</span><span class="hljs-keyword">new</span> <span class="hljs-title class_">Vue</span>(&#123;  <span class="hljs-attr">el</span>: <span class="hljs-string">&#x27;#app&#x27;</span>,  router,  <span class="hljs-attr">components</span>: &#123; <span class="hljs-title class_">App</span> &#125;,  <span class="hljs-attr">template</span>: <span class="hljs-string">&#x27;&lt;App/&gt;&#x27;</span>&#125;)<span class="hljs-comment">// runtime only的 main.js</span><span class="hljs-keyword">new</span> <span class="hljs-title class_">Vue</span>(&#123;  <span class="hljs-attr">el</span>: <span class="hljs-string">&#x27;#app&#x27;</span>,  router,  <span class="hljs-attr">render</span>: <span class="hljs-function"><span class="hljs-params">h</span> =&gt;</span> <span class="hljs-title function_">h</span>(<span class="hljs-title class_">App</span>)&#125;)<span class="hljs-comment">// cli4.0</span><span class="hljs-keyword">new</span> <span class="hljs-title class_">Vue</span>(&#123;  <span class="hljs-attr">render</span>: <span class="hljs-function"><span class="hljs-params">h</span> =&gt;</span> <span class="hljs-title function_">h</span>(<span class="hljs-title class_">App</span>),&#125;).$mount(<span class="hljs-string">&#x27;#app&#x27;</span>)</code></pre><h2 id="new-Vue-都干了什么"><a href="#new-Vue-都干了什么" class="headerlink" title="new Vue() 都干了什么"></a>new Vue() 都干了什么</h2><img src="/2021/07/01/p006/image-2.png" class="" title="alt"><p><code>_init</code> 是定义在原型上的方法，主要就干了几件事情：</p><ul><li>合并配置</li><li>初始化生命周期</li><li>初始化事件中心</li><li>初始化渲染</li><li>初始化 data、props、computed、watcher</li></ul><h2 id="实例挂载"><a href="#实例挂载" class="headerlink" title="实例挂载"></a>实例挂载</h2><p>$mount 方法实际上会去调用 mountComponent 方法，mountComponent 核心就是先实例化一个渲染 Watcher，在它的回调函数中会调用 updateComponent 方法，updateComponent 的核心是：</p><pre><code class="hljs js">vm.<span class="hljs-title function_">_update</span>(vm.<span class="hljs-title function_">_render</span>(), hydrating)</code></pre><p><code>vm._render</code> 最终是通过执行 createElement 方法并返回虚拟DOM，<code>_update</code> 方法是把虚拟DOM 渲染成真实的 DOM</p><img src="/2021/07/01/p006/image-3.png" class="" title="alt"><p><code>_update</code> 的核心就是调用 <code>vm.__patch__</code> 方法，<code>vm.__patch__</code> 在不同平台上定义是不同的，最终都是调用 createPatchFunction 返回一个 patch 方法在 <code>_update</code> 中调用<br>patch 中的重点是 createElm 方法，createElm 的作用是通过虚拟节点创建真实的 DOM 并插入到它的父节点中</p><h2 id="Virtual-DOM"><a href="#Virtual-DOM" class="headerlink" title="Virtual DOM"></a>Virtual DOM</h2><p>一个 dom 元素有很多属性，频繁的做 dom 更新，会有性能问题</p><p>dom元素的原型链：dom-&gt;HTMLDivElement-&gt;HTMLElement-&gt;Element-&gt;Node-&gt;EventTarget-&gt;Object.prototype-&gt;null</p><p>Virtual DOM 就是用一个原生的 JS 对象去描述一个 DOM 节点，在 Vue.js 中，Virtual DOM 是用  VNode 这么一个 Class 去描述。Virtual DOM 映射到真实的 DOM 要经历 VNode 的 create、diff、patch 等过程</p><pre><code class="hljs js"><span class="hljs-comment">// 真实 dom</span>&lt;div <span class="hljs-keyword">class</span>=<span class="hljs-string">&quot;parent&quot;</span> style=<span class="hljs-string">&quot;height:0&quot;</span> href=<span class="hljs-string">&quot;test&quot;</span>&gt;test&lt;/div&gt;<span class="hljs-comment">// 虚拟 dom</span>&#123;<span class="hljs-attr">tag</span>:<span class="hljs-string">&quot;test&quot;</span>,<span class="hljs-attr">data</span>:&#123;<span class="hljs-attr">attr</span>:&#123;<span class="hljs-attr">href</span>:<span class="hljs-string">&quot;test&quot;</span>&#125;,<span class="hljs-attr">staticClass</span>:<span class="hljs-string">&quot;parent&quot;</span>,<span class="hljs-attr">staticStyle</span>:&#123;<span class="hljs-attr">height</span>:<span class="hljs-number">0</span>&#125;&#125;,<span class="hljs-attr">children</span>:[&#123;<span class="hljs-attr">tag</span>:<span class="hljs-literal">undefined</span>,<span class="hljs-attr">text</span>:<span class="hljs-string">&quot;test&quot;</span>&#125;]&#125;</code></pre><h2 id="生命周期"><a href="#生命周期" class="headerlink" title="生命周期"></a>生命周期</h2><p>new Vue初始化时<br>    执行 beforeCreate、created 钩子函数<br>$mount挂载时<br>    执行 beforeMount，mounted 钩子函数</p><h2 id="响应式原理"><a href="#响应式原理" class="headerlink" title="响应式原理"></a>响应式原理</h2><p>Vue.js 实现响应式的核心是利用了 ES5 的  [[JS对象#Object.defineProperty()]]，给数据添加了 [[JS对象#getter&#x2F;setter]]，目的就是为了在我们访问数据以及写数据的时候能自动执行一些逻辑：getter 做的事情是[[#依赖收集]]，setter 做的事情是[[#派发更新]]，一旦对象拥有了 getter 和 setter，就可以简单地把这个对象称为响应式对象</p><p>在 <code>_init()</code> 中调用 initState() 去初始化 props、methods、data、computed 和 watch，将 props、data 变为响应式对象</p><pre><code class="hljs js"><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">initState</span> (<span class="hljs-params">vm: Component</span>) &#123;  vm.<span class="hljs-property">_watchers</span> = []  <span class="hljs-keyword">const</span> opts = vm.<span class="hljs-property">$options</span>  <span class="hljs-keyword">if</span> (opts.<span class="hljs-property">props</span>) <span class="hljs-title function_">initProps</span>(vm, opts.<span class="hljs-property">props</span>)  <span class="hljs-keyword">if</span> (opts.<span class="hljs-property">methods</span>) <span class="hljs-title function_">initMethods</span>(vm, opts.<span class="hljs-property">methods</span>)  <span class="hljs-keyword">if</span> (opts.<span class="hljs-property">data</span>) &#123;    <span class="hljs-title function_">initData</span>(vm)  &#125; <span class="hljs-keyword">else</span> &#123;    <span class="hljs-title function_">observe</span>(vm.<span class="hljs-property">_data</span> = &#123;&#125;, <span class="hljs-literal">true</span> <span class="hljs-comment">/* asRootData */</span>)  &#125;  <span class="hljs-keyword">if</span> (opts.<span class="hljs-property">computed</span>) <span class="hljs-title function_">initComputed</span>(vm, opts.<span class="hljs-property">computed</span>)  <span class="hljs-keyword">if</span> (opts.<span class="hljs-property">watch</span> &amp;&amp; opts.<span class="hljs-property">watch</span> !== nativeWatch) &#123;    <span class="hljs-title function_">initWatch</span>(vm, opts.<span class="hljs-property">watch</span>)  &#125;&#125;</code></pre><p>initProps() 通过 defineReactive() 把每个 prop 对应的值变成响应式<br>initData() 通过 observe() 观测整个 data 的变化，把 data 变成响应式</p><p>observe() 就是给数据实例化一个 Observer 对象实例，Observer 是一个类，它的作用是给对象的属性添加 getter 和 setter，用于依赖收集和派发更新。Observer 的构造函数对于数组会调用 observeArray()，对纯对象调用 walk()。observeArray 遍历数组再次调用 observe()，walk() 是遍历对象的 key 调用 defineReactive()</p><pre><code class="hljs js"><span class="hljs-keyword">export</span> <span class="hljs-keyword">function</span> <span class="hljs-title function_">defineReactive</span> (<span class="hljs-params"></span><span class="hljs-params">  obj: <span class="hljs-built_in">Object</span>,</span><span class="hljs-params">  key: string,</span><span class="hljs-params">  val: any,</span><span class="hljs-params">  customSetter?: ?<span class="hljs-built_in">Function</span>,</span><span class="hljs-params">  shallow?: boolean</span><span class="hljs-params"></span>) &#123;  <span class="hljs-keyword">const</span> dep = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Dep</span>()  <span class="hljs-keyword">const</span> property = <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">getOwnPropertyDescriptor</span>(obj, key)  <span class="hljs-keyword">if</span> (property &amp;&amp; property.<span class="hljs-property">configurable</span> === <span class="hljs-literal">false</span>) &#123;    <span class="hljs-keyword">return</span>  &#125;  <span class="hljs-keyword">const</span> getter = property &amp;&amp; property.<span class="hljs-property">get</span>  <span class="hljs-keyword">const</span> setter = property &amp;&amp; property.<span class="hljs-property">set</span>  <span class="hljs-keyword">if</span> ((!getter || setter) &amp;&amp; <span class="hljs-variable language_">arguments</span>.<span class="hljs-property">length</span> === <span class="hljs-number">2</span>) &#123;    val = obj[key]  &#125;  <span class="hljs-keyword">let</span> childOb = !shallow &amp;&amp; <span class="hljs-title function_">observe</span>(val)  <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>(obj, key, &#123;    <span class="hljs-attr">enumerable</span>: <span class="hljs-literal">true</span>,    <span class="hljs-attr">configurable</span>: <span class="hljs-literal">true</span>,    <span class="hljs-attr">get</span>: <span class="hljs-keyword">function</span> <span class="hljs-title function_">reactiveGetter</span> (<span class="hljs-params"></span>) &#123;      <span class="hljs-keyword">const</span> value = getter ? getter.<span class="hljs-title function_">call</span>(obj) : val      <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Dep</span>.<span class="hljs-property">target</span>) &#123;        dep.<span class="hljs-title function_">depend</span>()        <span class="hljs-keyword">if</span> (childOb) &#123;          childOb.<span class="hljs-property">dep</span>.<span class="hljs-title function_">depend</span>()          <span class="hljs-keyword">if</span> (<span class="hljs-title class_">Array</span>.<span class="hljs-title function_">isArray</span>(value)) &#123;            <span class="hljs-title function_">dependArray</span>(value)          &#125;        &#125;      &#125;      <span class="hljs-keyword">return</span> value    &#125;,    <span class="hljs-attr">set</span>: <span class="hljs-keyword">function</span> <span class="hljs-title function_">reactiveSetter</span> (<span class="hljs-params">newVal</span>) &#123;      <span class="hljs-keyword">const</span> value = getter ? getter.<span class="hljs-title function_">call</span>(obj) : val      <span class="hljs-keyword">if</span> (newVal === value || (newVal !== newVal &amp;&amp; value !== value)) &#123;        <span class="hljs-keyword">return</span>      &#125;      <span class="hljs-keyword">if</span> (process.<span class="hljs-property">env</span>.<span class="hljs-property">NODE_ENV</span> !== <span class="hljs-string">&#x27;production&#x27;</span> &amp;&amp; customSetter) &#123;        <span class="hljs-title function_">customSetter</span>()      &#125;      <span class="hljs-keyword">if</span> (getter &amp;&amp; !setter) <span class="hljs-keyword">return</span>      <span class="hljs-keyword">if</span> (setter) &#123;        setter.<span class="hljs-title function_">call</span>(obj, newVal)      &#125; <span class="hljs-keyword">else</span> &#123;        val = newVal      &#125;      childOb = !shallow &amp;&amp; <span class="hljs-title function_">observe</span>(newVal)      dep.<span class="hljs-title function_">notify</span>()    &#125;  &#125;)&#125;</code></pre><p>defineReactive 的功能就是定义一个响应式对象，利用 Object.defineProperty() 给对象动态添加 getter 和 setter。对于子对象，会递归调用 observe 方法，这样就保证了无论对象的结构多复杂，它的所有子属性也能变成响应式的对象</p><h2 id="依赖收集"><a href="#依赖收集" class="headerlink" title="依赖收集"></a>依赖收集</h2><p>在 defineReactive() 中，const dep &#x3D; new Dep() 实例化一个 Dep 的实例，get 函数中通过 dep.depend() 做依赖收集，依赖收集的 watcher 对象被保存在 Dep 的 subs 中，Dep 类有一个静态属性 target，是一个全局唯一的 Watcher，Dep 对 Watcher 进行管理</p><p>什么时候触发 getter 做依赖收集?<br>mount 时调用 mountComponent()，mountComponent 实例化一个渲染 watcher 时，首先进入 watcher 的构造函数中，构造函数执行 this.get() ，get 函数执行 this.getter()，这个 getter 函数就是 updateComponent 函数，即执行 <code>vm._update(vm._render(), hydrating)</code></p><img src="/2021/07/01/p006/image-4.png" class="" title="alt"><h2 id="派发更新"><a href="#派发更新" class="headerlink" title="派发更新"></a>派发更新</h2><p>在 defineReactive() 中，set 函数中通过 dep.notify() 做派发更新，subs 是依赖收集到的所有 watcher，在 notify 中对每一个 watcher 执行 update </p><pre><code class="hljs js"><span class="hljs-title function_">notify</span> () &#123;    <span class="hljs-keyword">const</span> subs = <span class="hljs-variable language_">this</span>.<span class="hljs-property">subs</span>.<span class="hljs-title function_">slice</span>()    <span class="hljs-keyword">if</span> (process.<span class="hljs-property">env</span>.<span class="hljs-property">NODE_ENV</span> !== <span class="hljs-string">&#x27;production&#x27;</span> &amp;&amp; !config.<span class="hljs-property">async</span>) &#123;      subs.<span class="hljs-title function_">sort</span>(<span class="hljs-function">(<span class="hljs-params">a, b</span>) =&gt;</span> a.<span class="hljs-property">id</span> - b.<span class="hljs-property">id</span>)    &#125;    <span class="hljs-keyword">for</span> (<span class="hljs-keyword">let</span> i = <span class="hljs-number">0</span>, l = subs.<span class="hljs-property">length</span>; i &lt; l; i++) &#123;      subs[i].<span class="hljs-title function_">update</span>()    &#125;  &#125;</code></pre><pre><code class="hljs js"><span class="hljs-title function_">update</span> () &#123;    <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">lazy</span>) &#123;      <span class="hljs-variable language_">this</span>.<span class="hljs-property">dirty</span> = <span class="hljs-literal">true</span>    &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (<span class="hljs-variable language_">this</span>.<span class="hljs-property">sync</span>) &#123;      <span class="hljs-variable language_">this</span>.<span class="hljs-title function_">run</span>()    &#125; <span class="hljs-keyword">else</span> &#123;      <span class="hljs-title function_">queueWatcher</span>(<span class="hljs-variable language_">this</span>)    &#125;  &#125;</code></pre>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> vue </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>javascript 面试题（持续更新）</title>
      <link href="/2021/06/01/p005/"/>
      <url>/2021/06/01/p005/</url>
      
        <content type="html"><![CDATA[<h2 id="数据类型相关"><a href="#数据类型相关" class="headerlink" title="数据类型相关"></a>数据类型相关</h2><h3 id="js有多少种数据类型"><a href="#js有多少种数据类型" class="headerlink" title="js有多少种数据类型"></a>js有多少种数据类型</h3><ul><li>7种原始数据类型：null、undefined、boolean、number、string、symbol（es6新增）、bigint（es10新增）</li><li>1种引用类型：object</li></ul><h3 id="Symbol的特点和作用"><a href="#Symbol的特点和作用" class="headerlink" title="Symbol的特点和作用"></a>Symbol的特点和作用</h3><ul><li>每个 symbol 实例值都是唯一的</li><li>不能通过 new 构造<pre><code class="hljs js"><span class="hljs-keyword">var</span> a = <span class="hljs-title class_">Symbol</span>();<span class="hljs-keyword">var</span> b = <span class="hljs-title class_">Symbol</span>();a == b  <span class="hljs-comment">// false</span><span class="hljs-keyword">var</span> c = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Symbol</span>()  <span class="hljs-comment">// Uncaught TypeError: Symbol is not a constructor</span></code></pre>应用场景：</li><li>作为对象属性名<br>Symbol 类型的属性时不可被枚举的，Object.keys()、for…in、Object.getOwnPropertyNames() 都不能枚举出 Symbol，利用该特性，我们可以把一些不需要对外操作和访问的属性使用 Symbol 来定义<pre><code class="hljs js"><span class="hljs-keyword">var</span> obj = &#123;<span class="hljs-attr">a</span>: <span class="hljs-number">1</span>,[<span class="hljs-title class_">Symbol</span>()]: <span class="hljs-string">&#x27;symbol&#x27;</span>&#125;<span class="hljs-title class_">Object</span>.<span class="hljs-title function_">keys</span>(obj);  <span class="hljs-comment">// [&#x27;a&#x27;]</span></code></pre></li><li>使用 Symbol 来替代常量</li><li>定义类的私有属性&#x2F;方法</li></ul><h3 id="BigInt的特点和作用"><a href="#BigInt的特点和作用" class="headerlink" title="BigInt的特点和作用"></a>BigInt的特点和作用</h3><p>BigInt 可以表示任意大的整数<br>可以在整数后面加 n 定义一个 BigInt （如10n），或调用 BigInt()（不需要new）<br>特定：</p><ul><li>bigint 不能用于 Math 对象中的方法</li><li>不能和 Number 实例混合运算</li><li>带小数的运算会被取整</li><li>BigInt 和 Number 不严格相等<br>应用场景：高精度时间戳，大整数</li></ul><h3 id="null和undefined有什么区别"><a href="#null和undefined有什么区别" class="headerlink" title="null和undefined有什么区别"></a>null和undefined有什么区别</h3><pre><code class="hljs js"><span class="hljs-keyword">typeof</span> <span class="hljs-literal">null</span>  <span class="hljs-comment">// &#x27;object&#x27;</span><span class="hljs-keyword">typeof</span> <span class="hljs-literal">undefined</span>  <span class="hljs-comment">// &#x27;undefined&#x27;</span><span class="hljs-literal">null</span> == <span class="hljs-literal">undefined</span>  <span class="hljs-comment">// true</span><span class="hljs-literal">null</span> === <span class="hljs-literal">undefined</span>  <span class="hljs-comment">// false</span><span class="hljs-title class_">Number</span>(<span class="hljs-literal">null</span>)  <span class="hljs-comment">// 0</span><span class="hljs-title class_">Number</span>(<span class="hljs-literal">undefined</span>)   <span class="hljs-comment">// NaN</span></code></pre><p>关于 typeof null 为 object 的 bug，早起版本用低位存储变量的类型，000表示对象，而 null 全为零，所以将 null 判断为了object<br>null 是空对象指针，转换为数值为0；undefined 是表示”无”的原始值，转为数值时为 NaN</p><h3 id="怎么判断数据类型"><a href="#怎么判断数据类型" class="headerlink" title="怎么判断数据类型"></a>怎么判断数据类型</h3><p>用 typeof 判断基本数据类型和函数，typeof function(){}  &#x2F;&#x2F; ‘function’<br>数组和对象用 typeof 操作结果都是 object，区分数组和对象的方式有：<br><strong>方式一：instanceof</strong></p><pre><code class="hljs js"><span class="hljs-keyword">typeof</span> &#123;&#125;  <span class="hljs-comment">// &#x27;object&#x27;</span><span class="hljs-keyword">typeof</span> []  <span class="hljs-comment">// &#x27;object&#x27;</span>(&#123;&#125;) <span class="hljs-keyword">instanceof</span> <span class="hljs-title class_">Object</span>  <span class="hljs-comment">// true</span>[] <span class="hljs-keyword">instanceof</span> <span class="hljs-title class_">Array</span>  <span class="hljs-comment">// true</span></code></pre><p>注意：{} instanceof Object会报错【Uncaught SyntaxError: Unexpected token ‘instanceof’】，因为{}也可看做代码块，应给{}加个括号，写成 ({}) instanceof Object</p><p><strong>方式二：constructor属性</strong></p><pre><code class="hljs js"><span class="hljs-keyword">var</span> arr = [<span class="hljs-number">1</span>,<span class="hljs-number">2</span>]arr.<span class="hljs-property">constructor</span> === <span class="hljs-title class_">Array</span>  <span class="hljs-comment">// true</span><span class="hljs-keyword">var</span> obj = &#123;&#125;obj.<span class="hljs-property">constructor</span> === <span class="hljs-title class_">Object</span>  <span class="hljs-comment">// true</span></code></pre><p><strong>方式三：Array.isArray()</strong></p><p><strong>方式四：[[JS对象#Object.prototype.toString.call()]] 可以区分所有类型</strong></p><pre><code class="hljs js"><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title class_">Object</span>.<span class="hljs-property"><span class="hljs-keyword">prototype</span></span>.<span class="hljs-property">toString</span>.<span class="hljs-title function_">call</span>(&#123;&#125;));  <span class="hljs-comment">// [object Object]</span><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-title class_">Object</span>.<span class="hljs-property"><span class="hljs-keyword">prototype</span></span>.<span class="hljs-property">toString</span>.<span class="hljs-title function_">call</span>([]));  <span class="hljs-comment">// [object Array]</span></code></pre><h3 id="怎么进行数据类型转换"><a href="#怎么进行数据类型转换" class="headerlink" title="怎么进行数据类型转换"></a>怎么进行数据类型转换</h3><ul><li>转换为数字：Number()、parseInt()、parseFloat()</li><li>转换为字符串：toString()、String()</li><li>转换为布尔：Boolean()<pre><code class="hljs js"><span class="hljs-title class_">Number</span>(<span class="hljs-literal">null</span>)  <span class="hljs-comment">// 0</span><span class="hljs-title class_">Number</span>(<span class="hljs-literal">undefined</span>)   <span class="hljs-comment">// NaN</span><span class="hljs-title class_">Number</span>(<span class="hljs-string">&#x27;&#x27;</span>)  <span class="hljs-comment">// 0</span><span class="hljs-title class_">Number</span>(<span class="hljs-number">3</span>-<span class="hljs-number">1</span>)  <span class="hljs-comment">// 2</span><span class="hljs-title class_">Number</span>(<span class="hljs-string">&#x27;3&#x27;</span>-<span class="hljs-string">&#x27;1&#x27;</span>)  <span class="hljs-comment">// 2  这样竟然也可以</span><span class="hljs-title class_">Number</span>(<span class="hljs-string">&#x27;1a&#x27;</span>)  <span class="hljs-comment">// NaN</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-literal">null</span>)  <span class="hljs-comment">// NaN</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-literal">undefined</span>)  <span class="hljs-comment">// NaN</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-string">&#x27;&#x27;</span>)  <span class="hljs-comment">// NaN</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-string">&#x27;3.9&#x27;</span>-<span class="hljs-string">&#x27;2&#x27;</span>)  <span class="hljs-comment">// 1</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-string">&#x27;a&#x27;</span>)  <span class="hljs-comment">// NaN</span><span class="hljs-built_in">parseInt</span>(<span class="hljs-string">&#x27;123abc&#x27;</span>)  <span class="hljs-comment">// 123</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-string">&#x27;&#x27;</span>)  <span class="hljs-comment">// false</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-string">&#x27;123&#x27;</span>)  <span class="hljs-comment">// true</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-string">&#x27;true&#x27;</span>)  <span class="hljs-comment">// true</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-string">&#x27;false&#x27;</span>)  <span class="hljs-comment">// true</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-string">&#x27;0&#x27;</span>)  <span class="hljs-comment">// true</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-number">0</span>)  <span class="hljs-comment">// false</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-literal">null</span>)  <span class="hljs-comment">// false</span><span class="hljs-title class_">Boolean</span>(<span class="hljs-literal">undefined</span>)  <span class="hljs-comment">// false</span></code></pre></li></ul><h3 id="NaN是什么值"><a href="#NaN是什么值" class="headerlink" title="NaN是什么值"></a>NaN是什么值</h3><p>Not a Number，是一个表示非数字的值，不可写不可枚举不可配置</p><pre><code class="hljs js"><span class="hljs-keyword">typeof</span> <span class="hljs-title class_">NaN</span> <span class="hljs-comment">// &#x27;number&#x27;</span><span class="hljs-title class_">NaN</span> <span class="hljs-keyword">instanceof</span> <span class="hljs-title class_">Number</span> <span class="hljs-comment">// false</span><span class="hljs-title class_">NaN</span> === <span class="hljs-title class_">NaN</span>  <span class="hljs-comment">// false</span></code></pre><p>判断 NaN，可以使用 isNaN() 或 Number.isNaN()，或者，因为 NaN 是唯一与自身不相等的值，所以可以执行类似 x !&#x3D;&#x3D; x 这样的自我比较</p><pre><code class="hljs js"><span class="hljs-built_in">isNaN</span>(<span class="hljs-title class_">NaN</span>); <span class="hljs-comment">// true</span><span class="hljs-built_in">isNaN</span>(<span class="hljs-title class_">Number</span>.<span class="hljs-property">NaN</span>); <span class="hljs-comment">// true</span><span class="hljs-title class_">Number</span>.<span class="hljs-built_in">isNaN</span>(<span class="hljs-title class_">NaN</span>); <span class="hljs-comment">// true</span><span class="hljs-comment">// isNaN()和Number.isNaN()之间有区别</span><span class="hljs-built_in">isNaN</span>(<span class="hljs-string">&quot;hello world&quot;</span>); <span class="hljs-comment">// true</span><span class="hljs-title class_">Number</span>.<span class="hljs-built_in">isNaN</span>(<span class="hljs-string">&quot;hello world&quot;</span>); <span class="hljs-comment">// false，仅当值为NaN才为true</span></code></pre><h2 id="原型、原型链、继承"><a href="#原型、原型链、继承" class="headerlink" title="原型、原型链、继承"></a>原型、原型链、继承</h2><h3 id="原型和原型链"><a href="#原型和原型链" class="headerlink" title="原型和原型链"></a>原型和原型链</h3><p>JavaScript 中的对象有一个特殊的  <code>[[Prototype]]</code>  内置属性，就是对其他对象的引用，如果在对象上没有找到需要的属性或方法，就会继续在 <code>[[Prototype]]</code> 关联的对象上查找，层层向上找直到找到或者到对象原型为null时结束，这一系列 <code>[[Prototype]]</code> 链接就是原型链</p><p><code>__proto__</code>  引用了 <code>[[Prototype]]</code>，可以通过 <code>__proto__.__proto__...</code> 来遍历原型链</p><p>所有普通的 <code>[[Prototype]]</code> 链最终都会指向内置的 Object.prototype</p><pre><code class="hljs plaintext">var obj = &#123;&#125;Object.__proto__ === Object.prototype  // trueObject.getPrototypeOf(obj) === Object.prototype  // trueObject.getPrototypeOf(Object.prototype)  // nullObject.getPrototypeOf(Function.prototype) === Object.prototype  // true</code></pre><p>当试图引用对象的属性时会触发 <code>[[Get]]</code> 操作，对于默认的 <code>[[Get]]</code>，第一步是检查对象本身是否具有这个属性，如果无法在对象本身找到，就会继续访问对象的 <code>[[Prototype]]</code> 链</p><pre><code class="hljs javascript"><span class="hljs-keyword">var</span> obj = &#123;    <span class="hljs-attr">id</span>: <span class="hljs-number">1</span>,    <span class="hljs-attr">name</span>: <span class="hljs-string">&#x27;xx&#x27;</span>&#125;<span class="hljs-comment">// Object.create()会创建一个对象并把这个对象的 [[Prototype]] 关联到指定的对象</span><span class="hljs-keyword">var</span> obj2 = <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">create</span>(obj) obj2.<span class="hljs-property">id</span> <span class="hljs-comment">// 1</span>obj2.<span class="hljs-property">name</span> <span class="hljs-comment">// &#x27;xx&#x27;</span></code></pre><p>以上例子，创建了一个关联到 obj 的对象 obj2，obj2 的 <code>[[Prototype]]</code> 指向 obj，obj2 本身是个空对象，访问 obj2.id 在 obj2 本身没有找到 id 这个属性，访问到了 <code>[[Prototype]]</code> 找到了这个属性</p><img src="/2021/06/01/p005/image.png" class="" title="alt"><h4 id="属性设置和屏蔽"><a href="#属性设置和屏蔽" class="headerlink" title="属性设置和屏蔽"></a>属性设置和屏蔽</h4><img src="/2021/06/01/p005/image-1.png" class="" title="alt"><pre><code class="hljs javascript"><span class="hljs-keyword">var</span> obj = &#123;    <span class="hljs-attr">a</span>: <span class="hljs-number">1</span>&#125;<span class="hljs-keyword">var</span> _obj = <span class="hljs-title class_">Object</span>.<span class="hljs-title function_">create</span>(obj)obj.<span class="hljs-property">b</span> = <span class="hljs-number">2</span>  <span class="hljs-comment">// obj上没有b这个属性，则添加b，此时obj -&gt; &#123; a: 1, b: 2 &#125;</span>_obj.<span class="hljs-property">a</span> = <span class="hljs-number">100</span><span class="hljs-comment">// _obj -&gt; &#123; a: 100 &#125;，obj -&gt; &#123; a: 1, b: 2 &#125; 此为上图的1</span><span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>(obj, <span class="hljs-string">&#x27;b&#x27;</span>, &#123;  <span class="hljs-attr">writable</span>: <span class="hljs-literal">false</span>&#125;)<span class="hljs-comment">// 将obj中的b设置为只读</span>_obj.<span class="hljs-property">b</span> = <span class="hljs-number">300</span><span class="hljs-comment">// _obj -&gt; &#123; a: 100 &#125;，此时不会在_obj上创建属性b</span><span class="hljs-title class_">Object</span>.<span class="hljs-title function_">defineProperty</span>(_obj, <span class="hljs-string">&#x27;b&#x27;</span>, &#123; <span class="hljs-attr">value</span>: <span class="hljs-number">200</span> &#125;)<span class="hljs-comment">// _obj -&gt; &#123; a: 100, b: 200 &#125;</span></code></pre><p>只读属性会阻止 <code>[[Prototype]]</code> 链下层隐式创建同名属性，这样做主要是为了模拟类属性的继承。但实际上并不会发生类似的继承复制，而且这个限制只存在于&#x3D;赋值中，使 Object.defineProperty() 并不会受到影响</p><h3 id="JS中的‘类’"><a href="#JS中的‘类’" class="headerlink" title="JS中的‘类’"></a>JS中的‘类’</h3><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">Foo</span>(<span class="hljs-params"></span>) &#123;  ...&#125;<span class="hljs-keyword">var</span> a = <span class="hljs-keyword">new</span> <span class="hljs-title class_">Foo</span>()<span class="hljs-title class_">Object</span>.<span class="hljs-title function_">getPrototypeOf</span>(a) === <span class="hljs-title class_">Foo</span>.<span class="hljs-property"><span class="hljs-keyword">prototype</span></span>; <span class="hljs-comment">// true</span>a.<span class="hljs-property">constructor</span> == <span class="hljs-title class_">Foo</span> <span class="hljs-comment">// true</span></code></pre><p>调用 new Foo() 时会创建 a，其中的一步就是给 a 一个内部的 <code>[[Prototype]]</code> 链接，关联到 Foo.prototype 指向的那个对象</p><ul><li>js 中只有对象</li><li>js 中没有复制机制</li><li>函数本身并不是构造函数，js 中的‘构造函数’可以理解为<code>带 new 的函数调用</code></li></ul><h3 id="ES6-的-Class"><a href="#ES6-的-Class" class="headerlink" title="ES6 的 Class"></a>ES6 的 Class</h3><p>ES6 的 Class 只是一个语法糖，是 ES5 的构造函数的一层包装</p><pre><code class="hljs plaintext">// es5function es5Foo(x, y) &#123;this.x = x;this.y = y;&#125;es5Foo.prototype.add = function() &#123;return this.x + this.y;&#125;var f1 = new es5Foo(1, 2);// es6class es6Foo &#123;constructor(x, y) &#123;this.x = x;this.y = y;&#125;add() &#123;return this.x + this.y;&#125;&#125;var f2 = new es6Foo(1, 2);</code></pre><p>类的所有方法都定义在类的 prototype 上，prototype 的 constructor 指向类本身，这与 ES5 一致</p><pre><code class="hljs plaintext">es5Foo.prototype.constructor === es5Foo  // trueObject.getPrototypeOf(f1) === es5Foo.prototype  // truees6Foo.prototype.constructor === es6Foo  // trueObject.getPrototypeOf(f2) === es6Foo.prototype  // true</code></pre><p>类的内部所有定义的方法都是不可枚举的，这与 ES5 不同</p><pre><code class="hljs plaintext">Object.keys(es5Foo.prototype)  // [&#x27;add&#x27;]Object.keys(es6Foo.prototype)  // []</code></pre><p> new.target： ES6 为 new 命令引入了一个 new.target 属性，该属性一般用在构造函数之中，返回 new 命令作用于的那个构造函数，如果构造函数不是通过 new 命令或 Reflect.construct() 调用的， new.target 会返回 undefined</p><ul><li>可以用来确保构造函数只能通过 new 调用</li><li>可以用来写不能单独使用、必须继承后才能用的类</li></ul><h3 id="原型继承"><a href="#原型继承" class="headerlink" title="原型继承"></a>原型继承</h3><p>js 中并没有复制机制，new 操作不是创建一个类的多个实例，而是可以创建多个对象并把它们的<code>[[Prototype]]</code> 关联到同一个对象，这个机制就是 js 中的原型继承，通过让一个对象的原型指向另一个对象实现继承</p><p>继承方式：</p><ul><li>原型链继承</li><li>构造函数继承</li><li>组合继承</li><li>原型式继承</li><li>寄生组合式继承</li></ul><h2 id="闭包"><a href="#闭包" class="headerlink" title="闭包"></a>闭包</h2><p>一个函数中嵌套一个内部函数，这个内部函数就形成了一个闭包，闭包可以在一个内层函数中访问到外层函数的作用域</p><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">func</span>(<span class="hljs-params"></span>) &#123;<span class="hljs-keyword">var</span> local = <span class="hljs-string">&#x27;123&#x27;</span>;  <span class="hljs-comment">// 在函数外部是访问不到的 </span><span class="hljs-keyword">function</span> <span class="hljs-title function_">innerF</span>(<span class="hljs-params"></span>) &#123;  <span class="hljs-comment">// 内部函数形成一个闭包</span><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(local)&#125;<span class="hljs-keyword">return</span> innerF;&#125;<span class="hljs-keyword">var</span> f = <span class="hljs-title function_">func</span>();<span class="hljs-title function_">f</span>();  <span class="hljs-comment">// 123</span></code></pre><pre><code class="hljs javascript"><span class="hljs-comment">// 经典循环问题</span><span class="hljs-keyword">for</span>( <span class="hljs-keyword">var</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">5</span>; i++ ) &#123;    <span class="hljs-built_in">setTimeout</span>(<span class="hljs-function">() =&gt;</span> &#123;        <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>( i );    &#125;, <span class="hljs-number">1000</span>)&#125; <span class="hljs-comment">// 输出5个5</span><span class="hljs-keyword">for</span> (<span class="hljs-keyword">var</span> i = <span class="hljs-number">0</span>; i &lt; <span class="hljs-number">5</span>; i++) &#123;    (<span class="hljs-keyword">function</span>(<span class="hljs-params">j</span>) &#123;        <span class="hljs-built_in">setTimeout</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params"></span>) &#123;            <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(j);        &#125;,<span class="hljs-number">1000</span>)    &#125;)(i)&#125; <span class="hljs-comment">// 0 1 2 3 4</span></code></pre><p>闭包的作用：创建私有变量，延长变量的生命周期</p><p>实例：</p><pre><code class="hljs javascript"><span class="hljs-keyword">var</span> makeCounter = <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;  <span class="hljs-keyword">var</span> privateCounter = <span class="hljs-number">0</span>;  <span class="hljs-keyword">function</span> <span class="hljs-title function_">changeBy</span>(<span class="hljs-params">val</span>) &#123;    privateCounter += val;  &#125;  <span class="hljs-keyword">return</span> &#123;    <span class="hljs-attr">increment</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;      <span class="hljs-title function_">changeBy</span>(<span class="hljs-number">1</span>);    &#125;,    <span class="hljs-attr">decrement</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;      <span class="hljs-title function_">changeBy</span>(-<span class="hljs-number">1</span>);    &#125;,    <span class="hljs-attr">value</span>: <span class="hljs-keyword">function</span> (<span class="hljs-params"></span>) &#123;      <span class="hljs-keyword">return</span> privateCounter;    &#125;,  &#125;;&#125;;<span class="hljs-keyword">var</span> <span class="hljs-title class_">Counter1</span> = <span class="hljs-title function_">makeCounter</span>();<span class="hljs-keyword">var</span> <span class="hljs-title class_">Counter2</span> = <span class="hljs-title function_">makeCounter</span>();</code></pre><p>两个计数器 Counter1 和 Counter2 维护它们各自的独立性，每个闭包都是引用自己词法作用域内的变量 privateCounter，不会相互影响</p><h2 id="事件循环"><a href="#事件循环" class="headerlink" title="事件循环"></a>事件循环</h2><p>事件循环负责执行代码、收集和处理事件以及执行队列中的子任务。</p><p>js 是单线程的，所有的任务需要排队，前一个任务结束，才会执行下一个任务。</p><ul><li>所有同步任务都在主线程上执行，形成一个执行栈</li><li>主线程外有一个任务队列</li><li>主线程的所有同步任务执行完成就会读取任务队列，异步任务进入主线程开始执行</li><li>主线程不断重复以上三步就是事件循环机制</li></ul><p>同步任务：即主线程上的任务，按照顺序由上⾄下依次执⾏，当前⼀个任务执⾏完毕后，才能执⾏下⼀个任务。<br>异步任务：不进⼊主线程，⽽是进⼊任务队列的任务，执行完毕之后会产生一个回调函数，并且通知主线程。当主线程上的任务执行完后，就会调取最早通知自己的回调函数，使其进入主线程中执行。</p><p>异步任务分为宏任务、微任务<br>宏任务：<br>()，setTimeout()，宏任务是宿主发起的，宏任务队列有多个<br>微任务：new Promise()，new MutationObserve()，微任务是 js 本身发起的，微任务队列只有一个</p><p>当前执行栈执行完毕后会立刻先处理所有微任务队列中的事件，然后再去宏任务队列中取出一个事件。同一次事件循环中，微任务永远在宏任务之前执行。</p><img src="/2021/06/01/p005/image-2.png" class="" title="alt"><h2 id="异步编程"><a href="#异步编程" class="headerlink" title="异步编程"></a>异步编程</h2><p>js 是单线程的，一次只能执行一个任务，同步执行效率低，异步主要解决了同步阻塞的问题</p><h3 id="回调"><a href="#回调" class="headerlink" title="回调"></a>回调</h3><p>回调函数是一段以参数形式传递给其他代码的可执行代码。</p><p>一般函数编写方和调用方都是我们自己，但是回调函数编写方是我们自己，但是调用方不是我们，而是我们引用的其他模块即第三方库，我们调用第三方库中的函数，并把回调函数传递给第三方，第三方中的函数调用我们编写的回调函数。第三方库并不清楚后续的具体实现，只能对外提供一个回调函数。</p><img src="/2021/06/01/p005/image-3.png" class="" title="alt"><p>同步回调：主程序等待回调函数的完成</p><img src="/2021/06/01/p005/image-4.png" class="" title="alt"><p>异步回调：主程序和回调函数同时运行，主程序和回调函数的执行位于不同的线程或进程中</p><img src="/2021/06/01/p005/image-5.png" class="" title="alt"><p>假设一个任务需要调用多个服务，每一个服务都依赖于上一个服务的结果，采用异步回调就会形成回调地狱</p><pre><code class="hljs javascript"><span class="hljs-comment">// 同步</span>a = <span class="hljs-title class_">GetServiceA</span>();b = <span class="hljs-title class_">GetServiceB</span>(a);c = <span class="hljs-title class_">GetServiceC</span>(b);d = <span class="hljs-title class_">GetServiceD</span>(c);<span class="hljs-comment">// 异步</span><span class="hljs-title class_">GetServiceA</span>(<span class="hljs-keyword">function</span>(<span class="hljs-params">a</span>)&#123;    <span class="hljs-title class_">GetServiceB</span>(a, <span class="hljs-keyword">function</span>(<span class="hljs-params">b</span>)&#123;        <span class="hljs-title class_">GetServiceC</span>(b, <span class="hljs-keyword">function</span>(<span class="hljs-params">c</span>)&#123;            <span class="hljs-title class_">GetServiceD</span>(c, <span class="hljs-keyword">function</span>(<span class="hljs-params">d</span>) &#123;                ....            &#125;);        &#125;);    &#125;);&#125;);</code></pre><h3 id="Promise"><a href="#Promise" class="headerlink" title="Promise"></a>Promise</h3><p>Promise 是异步编程的一种解决方案</p><p>Promise有三种状态：</p><ul><li>pending</li><li>fulfilled</li><li>rejected</li></ul><p>只有 pending-&gt;fulfilled 和 pending-&gt;rejected 的状态改变。只要处于fulfilled和rejected，状态就不会再变，即resolved</p><p>Promise一旦创建就无法取消，但可以中断后续的链式调用。当Promise链中抛出一个错误时，错误信息沿着链路向后传递，直至被捕获。利用这个特性能跳过链中函数的调用，直至链路终点，变相地结束Promise链</p><pre><code class="hljs javascript"><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">()=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;fulfilled1&#x27;</span>);    <span class="hljs-keyword">throw</span> <span class="hljs-string">&#x27;throw error&#x27;</span>;&#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">()=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;fulfilled2 这里不会打印&#x27;</span>);&#125;).<span class="hljs-title function_">catch</span>(<span class="hljs-function"><span class="hljs-params">err</span>=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;catch err&#x27;</span>,err);&#125;)</code></pre><p>但是，若链路中也对错误进行了捕获，则后续的函数会继续执行</p><pre><code class="hljs javascript"><span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>().<span class="hljs-title function_">then</span>(<span class="hljs-function">()=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;fulfilled1&#x27;</span>);    <span class="hljs-keyword">throw</span> <span class="hljs-string">&#x27;throw error&#x27;</span>;&#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">()=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;fulfilled2 这里不会打印&#x27;</span>);&#125;,<span class="hljs-function"><span class="hljs-params">err</span>=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;rejected2&#x27;</span>,err)  <span class="hljs-comment">// 链中捕获了错误</span>&#125;).<span class="hljs-title function_">then</span>(<span class="hljs-function">()=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;fulfilled3&#x27;</span>)  <span class="hljs-comment">// 此处会打印</span>&#125;).<span class="hljs-title function_">catch</span>(<span class="hljs-function"><span class="hljs-params">err</span>=&gt;</span>&#123;    <span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&#x27;catch err&#x27;</span>,err)  <span class="hljs-comment">// 此处捕获不到错误</span>&#125;)</code></pre><p>并发异步操作：</p><ul><li>Promise.all()：如果数组中的某个 Promise 被拒绝，Promise.all() 就会立即拒绝返回的 Promise，并终止其他操作</li><li>Promise.allSettled()：等待所有输入的 Promise 完成，不管其中是否有 Promise 被拒绝</li><li>Promise.any()：返回第一个兑现的 Promise，不会等待其他 Promise 完成</li><li>Promise.race()：返回第一个敲定的 Promise，无论兑现还是拒绝</li></ul><p><strong>手写 Promise.all</strong></p><pre><code class="hljs javascript"><span class="hljs-keyword">function</span> <span class="hljs-title function_">promiseAll</span>(<span class="hljs-params">promises</span>) &#123;<span class="hljs-keyword">return</span> <span class="hljs-keyword">new</span> <span class="hljs-title class_">Promise</span>(<span class="hljs-function">(<span class="hljs-params">resolve, reject</span>) =&gt;</span> &#123;<span class="hljs-keyword">let</span> arr = [];  <span class="hljs-comment">// 存放各promise的结果</span><span class="hljs-keyword">let</span> counter = <span class="hljs-number">0</span>;  <span class="hljs-comment">// 计数器</span>promises.<span class="hljs-title function_">forEach</span>(<span class="hljs-function">(<span class="hljs-params">item, i</span>) =&gt;</span> &#123;<span class="hljs-title class_">Promise</span>.<span class="hljs-title function_">resolve</span>(item).<span class="hljs-title function_">then</span>(<span class="hljs-function"><span class="hljs-params">res</span> =&gt;</span> &#123;arr[i] = res;<span class="hljs-comment">// 因为promise是异步的，用计数器保证每个promise都完成后再返回结果</span>counter++;<span class="hljs-keyword">if</span>(counter == promises.<span class="hljs-property">length</span>) &#123;<span class="hljs-title function_">resolve</span>(arr);&#125;&#125;).<span class="hljs-title function_">catch</span>(reject)&#125;)&#125;)&#125;</code></pre><h3 id="Generator"><a href="#Generator" class="headerlink" title="Generator"></a>Generator</h3><p><code>function*</code> 这种声明方式会定义一个<strong>生成器函数</strong>，它返回一个 Generator对象<br>yield关键字用于暂停和恢复<a href="https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference/Statements/function*">生成器函数</a></p><pre><code class="hljs javascript"><span class="hljs-keyword">function</span>* <span class="hljs-title function_">generator</span>(<span class="hljs-params">i</span>) &#123;  <span class="hljs-keyword">yield</span> i;  <span class="hljs-keyword">yield</span> i + <span class="hljs-number">10</span>;&#125;<span class="hljs-keyword">const</span> gen = <span class="hljs-title function_">generator</span>(<span class="hljs-number">10</span>);<span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(gen.<span class="hljs-title function_">next</span>().<span class="hljs-property">value</span>);<span class="hljs-comment">// Expected output: 10</span><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(gen.<span class="hljs-title function_">next</span>().<span class="hljs-property">value</span>);<span class="hljs-comment">// Expected output: 20</span></code></pre><h3 id="async-await"><a href="#async-await" class="headerlink" title="async&#x2F;await"></a>async&#x2F;await</h3><p>async&#x2F;await 是ES2017(ES8) 提出的<strong>基于 Promise</strong> 的解决异步的最终方案</p><ul><li>使用 async 声明一个函数时，该函数自动返回一个 Promise 对象</li><li>当遇到 await 时， async 函数会暂停执行，等待 Promise 的结果</li><li>等待期间，剩余操作会被挂起，被安排在微任务队列中等待处理</li><li>当 Promise 被解决或拒绝时，async 函数恢复执行，执行栈为空时，事件循环检查微任务队列，并执行其中的任务</li></ul>]]></content>
      
      
      <categories>
          
          <category> 前端 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> javascript </tag>
            
            <tag> 面试 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>不要再把 __dirname、__filename 当全局变量了！</title>
      <link href="/2021/05/19/p004/"/>
      <url>/2021/05/19/p004/</url>
      
        <content type="html"><![CDATA[<h2 id="dirname、-filename-到底是不是全局变量？"><a href="#dirname、-filename-到底是不是全局变量？" class="headerlink" title="__dirname、__filename 到底是不是全局变量？"></a>__dirname、__filename 到底是不是全局变量？</h2><p>node.js 官网给出的答案：不是！</p><img src="/2021/05/19/p004/image.png" class="" title="alt"><h2 id="模块作用域"><a href="#模块作用域" class="headerlink" title="模块作用域"></a>模块作用域</h2><p>__dirname、__filename 其实是模块作用域</p><img src="/2021/05/19/p004/image-1.png" class="" title="alt"><ul><li>__dirname：是当前模块的目录名</li><li>__filename：是当前模块的文件名</li><li>exports：对module.exports 的引用</li><li>module：对当前模块的引用</li><li>require：用于导入模块、JSON 和本地文件</li></ul><pre><code class="hljs js"><span class="hljs-comment">// test.js 位于 E:\projects\node\node-demo目录下，在当前目录执行 node test.js</span><span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;__dirname&quot;</span>, __dirname);<span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;__filename&quot;</span>, __filename);<span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;exports&quot;</span>, <span class="hljs-built_in">exports</span>);<span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;module&quot;</span>, <span class="hljs-variable language_">module</span>);<span class="hljs-variable language_">console</span>.<span class="hljs-title function_">log</span>(<span class="hljs-string">&quot;require&quot;</span>, <span class="hljs-built_in">require</span>);</code></pre><p>打印结果如下：</p><img src="/2021/05/19/p004/image-2.png" class="" title="alt"><h2 id="为什么可以直接取到-dirname、-filename-？"><a href="#为什么可以直接取到-dirname、-filename-？" class="headerlink" title="为什么可以直接取到 __dirname、__filename ？"></a>为什么可以直接取到 __dirname、__filename ？</h2><p>既然 __dirname、__filename 不是全局变量，那它们哪来的？为什么可以直接取到？<br>来，上源码</p><pre><code class="hljs js"><span class="hljs-comment">// node\lib\internal\modules\cjs\loader.js</span><span class="hljs-title class_">Module</span>.<span class="hljs-property"><span class="hljs-keyword">prototype</span></span>.<span class="hljs-property">_compile</span> = <span class="hljs-keyword">function</span>(<span class="hljs-params">content, filename</span>) &#123;  ......  <span class="hljs-keyword">const</span> compiledWrapper = <span class="hljs-title function_">wrapSafe</span>(filename, content, <span class="hljs-variable language_">this</span>);  ......&#125;;<span class="hljs-keyword">let</span> wrap = <span class="hljs-keyword">function</span>(<span class="hljs-params">script</span>) &#123; <span class="hljs-comment">// eslint-disable-line func-style</span>  <span class="hljs-keyword">return</span> <span class="hljs-title class_">Module</span>.<span class="hljs-property">wrapper</span>[<span class="hljs-number">0</span>] + script + <span class="hljs-title class_">Module</span>.<span class="hljs-property">wrapper</span>[<span class="hljs-number">1</span>];&#125;;<span class="hljs-keyword">const</span> wrapper = [  <span class="hljs-string">&#x27;(function (exports, require, module, __filename, __dirname) &#123; &#x27;</span>,  <span class="hljs-string">&#x27;\n&#125;);&#x27;</span>,];<span class="hljs-keyword">function</span> <span class="hljs-title function_">wrapSafe</span>(<span class="hljs-params">filename, content, cjsModuleInstance, codeCache</span>) &#123;  ......  <span class="hljs-keyword">if</span> (patched) &#123;    <span class="hljs-keyword">const</span> wrapped = <span class="hljs-title class_">Module</span>.<span class="hljs-title function_">wrap</span>(content);    <span class="hljs-keyword">const</span> script = <span class="hljs-title function_">makeContextifyScript</span>(      wrapped,                 <span class="hljs-comment">// code</span>      filename,                <span class="hljs-comment">// filename</span>      <span class="hljs-number">0</span>,                       <span class="hljs-comment">// lineOffset</span>      <span class="hljs-number">0</span>,                       <span class="hljs-comment">// columnOffset</span>      <span class="hljs-literal">undefined</span>,               <span class="hljs-comment">// cachedData</span>      <span class="hljs-literal">false</span>,                   <span class="hljs-comment">// produceCachedData</span>      <span class="hljs-literal">undefined</span>,               <span class="hljs-comment">// parsingContext</span>      hostDefinedOptionId,     <span class="hljs-comment">// hostDefinedOptionId</span>      importModuleDynamically, <span class="hljs-comment">// importModuleDynamically</span>    );    ......    <span class="hljs-keyword">return</span> <span class="hljs-title function_">runScriptInThisContext</span>(script, <span class="hljs-literal">true</span>, <span class="hljs-literal">false</span>);  &#125;  <span class="hljs-keyword">const</span> params = [ <span class="hljs-string">&#x27;exports&#x27;</span>, <span class="hljs-string">&#x27;require&#x27;</span>, <span class="hljs-string">&#x27;module&#x27;</span>, <span class="hljs-string">&#x27;__filename&#x27;</span>, <span class="hljs-string">&#x27;__dirname&#x27;</span> ];  <span class="hljs-keyword">try</span> &#123;    <span class="hljs-keyword">const</span> result = <span class="hljs-title function_">internalCompileFunction</span>(      content,                           <span class="hljs-comment">// code,</span>      filename,                          <span class="hljs-comment">// filename</span>      <span class="hljs-number">0</span>,                                 <span class="hljs-comment">// lineOffset</span>      <span class="hljs-number">0</span>,                                 <span class="hljs-comment">// columnOffset,</span>      codeCache,                         <span class="hljs-comment">// cachedData</span>      <span class="hljs-literal">false</span>,                             <span class="hljs-comment">// produceCachedData</span>      <span class="hljs-literal">undefined</span>,                         <span class="hljs-comment">// parsingContext</span>      <span class="hljs-literal">undefined</span>,                         <span class="hljs-comment">// contextExtensions</span>      params,                            <span class="hljs-comment">// params</span>      hostDefinedOptionId,               <span class="hljs-comment">// hostDefinedOptionId</span>      importModuleDynamically,           <span class="hljs-comment">// importModuleDynamically</span>    );    ......    <span class="hljs-keyword">return</span> result.<span class="hljs-property">function</span>;  &#125; <span class="hljs-keyword">catch</span> (err) &#123;    ......  &#125;&#125;</code></pre><p>重点就是 wrapSafe()、wrap()、wrapper<br>编译时，_compile 调用 wrapSafe ，wrapSafe 这个函数将 content 包装在一个脚本（wrapper）中，并在新的上下文中运行它，这个 content 就是我们写的 js 文件中的内容<br>比如 app.js 中有代码 console.log(__dirname);<br>当执行 node app.js，开始编译这个文件，wrapSafe 函数会将这个 js 文件中的所有内容先拼接成 <strong>‘(function (exports, require, module, __filename, __dirname) { console.log(__dirname); \n});’</strong> 这样一个字符串（即调用 Module.wrap(content)），再将它转化为一个可执行脚本（即调用 makeContextifyScript），然后在当前上下文中执行（即调用 runScriptInThisContext）<br>由源码可以看到，exports, require, module, __filename, __dirname 就是 wrapSafe 这个函数的参数，我们的代码其实就是在这个函数中运行，所以可以直接取到它们的值   </p>]]></content>
      
      
      <categories>
          
          <category> node </category>
          
      </categories>
      
      
        <tags>
            
            <tag> node </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>你搞懂module.exports和exports了吗？</title>
      <link href="/2021/04/22/p003/"/>
      <url>/2021/04/22/p003/</url>
      
        <content type="html"><![CDATA[<p>为了更好的理解 module.exports 和 exports 的关系，我们先来巩固下 js 的基础：</p><pre><code class="hljs css">// test<span class="hljs-selector-class">.js</span><span class="hljs-selector-tag">var</span> <span class="hljs-selector-tag">a</span> = &#123;name: <span class="hljs-number">1</span>&#125;;<span class="hljs-selector-tag">var</span> <span class="hljs-selector-tag">b</span> = <span class="hljs-selector-tag">a</span>;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">a</span>);  // &#123; name: <span class="hljs-number">1</span> &#125;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">b</span>);  // &#123; name: <span class="hljs-number">1</span> &#125; <span class="hljs-selector-tag">b</span><span class="hljs-selector-class">.name</span> = <span class="hljs-number">2</span>;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">a</span>);  // &#123; name: <span class="hljs-number">2</span> &#125;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">b</span>);  // &#123; name: <span class="hljs-number">2</span> &#125; <span class="hljs-selector-tag">var</span> <span class="hljs-selector-tag">b</span> = &#123;name: <span class="hljs-number">3</span>&#125;;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">a</span>);  // &#123; name: <span class="hljs-number">2</span> &#125;console<span class="hljs-selector-class">.log</span>(<span class="hljs-selector-tag">b</span>);  // &#123; name: <span class="hljs-number">3</span> &#125;</code></pre><p>a 是一个对象，b 是对 a 的引用，即 a 和 b 指向同一块内存，所以前两个输出一样。当对 b 作修改时，即 a 和 b 指向同一块内存地址的内容发生了改变，所以 a 也会体现出来，所以第三四个输出一样。当 b 被覆盖时，b 指向了一块新的内存，a 还是指向原来的内存，所以最后两个输出不一样。<br>明白了上述例子后，我们只需知道三点就知道 exports 和 module.exports 的区别了：</p><ul><li>module.exports 初始值为一个空对象 {}</li><li>exports 是指向的 module.exports 的引用</li><li>require() 返回的是 module.exports 而不是 exports</li></ul><p>我们会看到这样的写法：</p><pre><code class="hljs js"><span class="hljs-built_in">exports</span> = <span class="hljs-variable language_">module</span>.<span class="hljs-property">exports</span> = xxx</code></pre><p>上面的代码等价于:</p><pre><code class="hljs js"><span class="hljs-variable language_">module</span>.<span class="hljs-property">exports</span> = xxx<span class="hljs-built_in">exports</span> = <span class="hljs-variable language_">module</span>.<span class="hljs-property">exports</span></code></pre><p>原理很简单，即 module.exports 指向新的对象时，exports 断开了与 module.exports 的引用，那么通过 exports &#x3D; module.exports 让 exports 重新指向 module.exports 即可。<br>系统自动给 nodejs 文件增加2个变量 exports 和 module，module 又有一个属性 exports，这个exports 属性指向一个空对象 {}，同时 exports这个变量也指向了这个空对象{}，于是就有了 exports &#x3D;&gt; {} &lt;&#x3D;module.exports<br>这2个 exports 其实是没有直接关系的，唯一的关系是他们初始都指向同一个空对象{}，如果其中一个不指向做个空对象了，那么他们的关系就没有了</p><img src="/2021/04/22/p003/image.png" class="" title="alt">]]></content>
      
      
      <categories>
          
          <category> node </category>
          
      </categories>
      
      
        <tags>
            
            <tag> node </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>node中使用jwt实现token身份验证</title>
      <link href="/2021/04/08/p002/"/>
      <url>/2021/04/08/p002/</url>
      
        <content type="html"><![CDATA[<h2 id="Cookie认证机制"><a href="#Cookie认证机制" class="headerlink" title="Cookie认证机制"></a>Cookie认证机制</h2><ol><li>用户向服务器发送用户名和密码</li><li>服务器验证通过后，在当前对话（session）里面保存相关数据，比如用户角色、登录时间等等</li><li>服务器向用户返回一个session_id，写入用户的Cookie</li><li>用户随后的每一次请求，都会通过 Cookie，将 session_id 传回服务器</li><li>服务器收到 session_id，找到前期保存的数据，由此得知用户的身份</li></ol><h2 id="Token认证机制"><a href="#Token认证机制" class="headerlink" title="Token认证机制"></a>Token认证机制</h2><p>我之前一直以为cookie和token是一样的，就是后台返回一个字符串保存在前端，之后的请求都带着这个字符串，后台去做校验，深入了解后知道两者是有很大区别的，是两种不同的机制。<br>cookie认证需要后台存一份session_id到数据库，多服务器时需要session共享。而token认证则不需要后台保存，token一般放在HTTP请求头的Authorization中。</p><h2 id="JWT"><a href="#JWT" class="headerlink" title="JWT"></a>JWT</h2><p>JSON Web Token (JWT)是一种开放标准，使用数字签名安全传输信息。JWT常用场景：授权（Authorization）和信息交换（Information Exchange）。授权是最常用JWT的场景。<br>JWT就是一个由‘.’分隔的字符串，这个字符串包含三个部分：Header、Payload、Signature。因此JWT的形式就是xxxxx.yyyyy.zzzzz。</p><h2 id="jwt实现token身份验证"><a href="#jwt实现token身份验证" class="headerlink" title="jwt实现token身份验证"></a>jwt实现token身份验证</h2><ol><li><p>在express项目中安装jsonwebtoken依赖</p><pre><code class="hljs plaintext">npm i jsonwebtoken --save</code></pre></li><li><p>新建authorization.js</p><pre><code class="hljs plaintext">const jwt = require(&quot;jsonwebtoken&quot;);const secretKey = &quot;secretKey&quot;;// 生成tokenmodule.exports.generateToken = function (payload) &#123;   const token =    &quot;Bearer &quot; +    jwt.sign(payload, secretKey, &#123;      expiresIn: 60 * 60,    &#125;);  return token;&#125;;// 验证tokenmodule.exports.verifyToken = function (req, res, next) &#123;  const token = req.headers.authorization.split(&quot; &quot;)[1];  jwt.verify(token, secretKey, function (err, decoded) &#123;    if (err) &#123;      console.log(&quot;verify error&quot;, err);      return res.json(&#123; code: &quot;404&quot;, msg: &quot;token无效&quot; &#125;);    &#125;    console.log(&quot;verify decoded&quot;, decoded);    next();  &#125;);&#125;;</code></pre><p><strong>注意：生成 token 时加了前缀’Bearer ‘，验证时要把’Bearer ‘去掉， req.headers.authorization.split(“ “)[1]，不然会出现JsonWebTokenError: invalid token的错误，验证失败。</strong></p></li><li><p>登录接口生成token返回给前端</p><pre><code class="hljs plaintext">// login.jsconst express = require(&quot;express&quot;);const router = express.Router();const &#123; generateToken &#125; = require(&quot;./authorization&quot;);// 路由router.post(&quot;/&quot;, (req, res) =&gt; &#123;  const username = req.body.username;  const password = req.body.password;  const token = generateToken(&#123; username: username &#125;);  res.json(&#123;    code: 200,    msg: &quot;登录成功&quot;,    data: &#123; token &#125;,  &#125;);&#125;);module.exports = router;</code></pre></li><li><p>在app.js中注册中间件</p><pre><code class="hljs plaintext">const loginRouter = require(&quot;./login&quot;);const auth = require(&quot;./authorization&quot;);const userRouter = require(&quot;./user&quot;);app.use(&quot;/api/login&quot;, loginRouter);app.use(&quot;/api/*&quot;, auth.verifyToken); // 注册token验证中间件app.use(&quot;/api/user&quot;, userRouter);</code></pre><p><strong>注意：验证token的中间件要放在login路由之后，其他需要验证的路由之前</strong></p></li><li><p>验证接口</p></li></ol><p>登录：</p><img src="/2021/04/08/p002/image.png" class="" title="登录"><p>获取用户列表：</p><img src="/2021/04/08/p002/image-1.png" class="" title="获取用户列表"><p>无效token：</p><img src="/2021/04/08/p002/image-2.png" class="" title="无效token"><h2 id="参考链接"><a href="#参考链接" class="headerlink" title="参考链接"></a>参考链接</h2><p><a href="https://jwt.io/introduction">JWT官网</a></p><p><a href="https://github.com/auth0/node-jsonwebtoken">node-jsonwebtoken</a></p><p><a href="https://www.ruanyifeng.com/blog/2018/07/json_web_token-tutorial.html">阮一峰-JSON Web Token 入门教程</a></p>]]></content>
      
      
      <categories>
          
          <category> node </category>
          
      </categories>
      
      
        <tags>
            
            <tag> node </tag>
            
            <tag> jwt </tag>
            
            <tag> cookie </tag>
            
            <tag> token </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>一篇带你使用 github pages + hexo 快速搭建个人博客</title>
      <link href="/2021/03/28/p001/"/>
      <url>/2021/03/28/p001/</url>
      
        <content type="html"><![CDATA[<h1 id="一、创建仓库并发布"><a href="#一、创建仓库并发布" class="headerlink" title="一、创建仓库并发布"></a>一、创建仓库并发布</h1><h2 id="1-创建一个名为-github-io-的仓库"><a href="#1-创建一个名为-github-io-的仓库" class="headerlink" title="1. 创建一个名为 &lt;账户名&gt;.github.io 的仓库"></a>1. 创建一个名为 <code>&lt;账户名&gt;.github.io</code> 的仓库</h2><img src="/2021/03/28/p001/image.png" class="" title="alt"><h2 id="2-新建-index-html-文件"><a href="#2-新建-index-html-文件" class="headerlink" title="2. 新建 index.html 文件"></a>2. 新建 <code>index.html</code> 文件</h2><p>内容为：</p><pre><code class="hljs html"><span class="hljs-comment">&lt;!-- index.html --&gt;</span><span class="hljs-meta">&lt;!DOCTYPE <span class="hljs-keyword">html</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">html</span> <span class="hljs-attr">lang</span>=<span class="hljs-string">&quot;en&quot;</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">head</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">meta</span> <span class="hljs-attr">charset</span>=<span class="hljs-string">&quot;UTF-8&quot;</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">title</span>&gt;</span>my blog<span class="hljs-tag">&lt;/<span class="hljs-name">title</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">head</span>&gt;</span><span class="hljs-tag">&lt;<span class="hljs-name">body</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>this is my blog<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span>    <span class="hljs-tag">&lt;<span class="hljs-name">h1</span>&gt;</span>Hello ~<span class="hljs-tag">&lt;/<span class="hljs-name">h1</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">body</span>&gt;</span><span class="hljs-tag">&lt;/<span class="hljs-name">html</span>&gt;</span></code></pre><img src="/2021/03/28/p001/image-1.png" class="" title="alt"><img src="/2021/03/28/p001/image-2.png" class="" title="alt"><h2 id="3-访问主页"><a href="#3-访问主页" class="headerlink" title="3. 访问主页"></a>3. 访问主页</h2><img src="/2021/03/28/p001/image-3.png" class="" title="alt"><img src="/2021/03/28/p001/image-4.png" class="" title="alt"><h2 id="4-搭建-hexo"><a href="#4-搭建-hexo" class="headerlink" title="4. 搭建 hexo"></a>4. 搭建 hexo</h2><ol><li>安装 hexo<pre><code class="hljs cmd">npm install -g hexo-clihexo init &lt;project-name&gt;<span class="hljs-built_in">cd</span> &lt;project-name&gt;npm install</code></pre></li><li>本地启动<pre><code class="hljs cmd">hexo g &amp;&amp; hexo s</code></pre>控制台看到如下即启动成功<img src="/2021/03/28/p001/image-9.png" class="" title="alt">访问 <a href="http://localhost:4000/">http://localhost:4000/</a><img src="/2021/03/28/p001/image-5.png" class="" title="alt"></li></ol><h2 id="5-将-hexo-发布到-github-pages"><a href="#5-将-hexo-发布到-github-pages" class="headerlink" title="5. 将 hexo 发布到 github pages"></a>5. 将 hexo 发布到 github pages</h2><ol><li>安装 <code>hexo-deployer-git</code><pre><code class="hljs cmd">npm install hexo-deployer-git --save</code></pre></li><li>在 <code>_config.yml</code> 中添加以下配置<pre><code class="hljs yml"><span class="hljs-attr">deploy:</span>  <span class="hljs-attr">type:</span> <span class="hljs-string">git</span>  <span class="hljs-attr">repo:</span> <span class="hljs-string">git@github.com:mmmying/mmmying.github.io.git</span>  <span class="hljs-attr">branch:</span> <span class="hljs-string">main</span></code></pre><img src="/2021/03/28/p001/image-6.png" class="" title="alt"></li><li>执行 <code>hexo clean &amp;&amp; hexo deploy</code> </li><li>浏览 <code>&lt;username&gt;.github.io</code>，检查网站能否运作<img src="/2021/03/28/p001/image-7.png" class="" title="alt">手机上访问效果<img src="/2021/03/28/p001/image-8.png" class="" title="alt"></li></ol><h1 id="二、修改主题"><a href="#二、修改主题" class="headerlink" title="二、修改主题"></a>二、修改主题</h1><h2 id="1-安装主题"><a href="#1-安装主题" class="headerlink" title="1. 安装主题"></a>1. 安装主题</h2><p>在 <a href="https://hexo.io/themes/">https://hexo.io/themes/</a> 选择一个喜欢的主题，比如 Claudia<br>在 hexo-blog 目录下执行</p><pre><code class="hljs plaintext">git clone https://github.com/Haojen/hexo-theme-Claudia.git themes/Claudia</code></pre><h2 id="2-修改主题配置"><a href="#2-修改主题配置" class="headerlink" title="2. 修改主题配置"></a>2. 修改主题配置</h2><p>修改 hexo-blog 目录下的 <code>_config.yml</code> 文件中的主题</p><pre><code class="hljs plaintext">// _config.ymltheme: Claudia</code></pre><h2 id="3-重新启动"><a href="#3-重新启动" class="headerlink" title="3.重新启动"></a>3.重新启动</h2><pre><code class="hljs plaintext">hexo ghexo s</code></pre><p>访问 <a href="http://localhost:4000/">http://localhost:4000/</a> </p><img src="/2021/03/28/p001/image-10.png" class="" title="alt">]]></content>
      
      
      
        <tags>
            
            <tag> github </tag>
            
            <tag> hexo </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
